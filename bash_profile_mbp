export PS1="\u@\h:\w ζ "
export PATH=$PATH:usr/local/Cellar/gcc/8.3.0/bin

# LS Colors
export LSCOLORS="exfxcxdxbxxxexgxgxexex"

# Aliases
alias ll="ls -lGH"
alias ls="ls -GH"
alias ca="conda activate"
alias cdd="conda deactivate"

# added by Anaconda3 2019.03 installer
# >>> conda init >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$(CONDA_REPORT_ERRORS=false '/Users/mppooser/anaconda3/bin/conda' shell.bash hook 2> /dev/null)"
if [ $? -eq 0 ]; then
    \eval "$__conda_setup"
else
    if [ -f "/Users/mppooser/anaconda3/etc/profile.d/conda.sh" ]; then
        . "/Users/mppooser/anaconda3/etc/profile.d/conda.sh"
        CONDA_CHANGEPS1=false conda activate base
    else
        \export PATH="/Users/mppooser/anaconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda init <<<
